--- configure	2020-01-13 20:24:33.000000000 +0000
+++ configure.2	2020-05-20 09:52:05.000000000 +0100
@@ -25466,12 +25466,12 @@
     pkg_cv_FRAME_CFLAGS="$FRAME_CFLAGS"
  elif test -n "$PKG_CONFIG"; then
     if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"libframe\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "libframe") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"framel\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "framel") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_FRAME_CFLAGS=`$PKG_CONFIG --cflags "libframe" 2>/dev/null`
+  pkg_cv_FRAME_CFLAGS=`$PKG_CONFIG --cflags "framel" 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
   pkg_failed=yes
@@ -25483,12 +25483,12 @@
     pkg_cv_FRAME_LIBS="$FRAME_LIBS"
  elif test -n "$PKG_CONFIG"; then
     if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"libframe\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "libframe") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"framel\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "framel") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_FRAME_LIBS=`$PKG_CONFIG --libs "libframe" 2>/dev/null`
+  pkg_cv_FRAME_LIBS=`$PKG_CONFIG --libs "framel" 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
   pkg_failed=yes
@@ -25509,9 +25509,9 @@
         _pkg_short_errors_supported=no
 fi
         if test $_pkg_short_errors_supported = yes; then
-	        FRAME_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors --cflags --libs "libframe" 2>&1`
+	        FRAME_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors --cflags --libs "framel" 2>&1`
         else
-	        FRAME_PKG_ERRORS=`$PKG_CONFIG --print-errors --cflags --libs "libframe" 2>&1`
+	        FRAME_PKG_ERRORS=`$PKG_CONFIG --print-errors --cflags --libs "framel" 2>&1`
         fi
 	# Put the nasty error message in config.log where it belongs
 	echo "$FRAME_PKG_ERRORS" >&5
@@ -25625,7 +25625,7 @@
   return 0;
 }
 _ACEOF
-for ac_lib in '' Frame; do
+for ac_lib in '' framel; do
   if test -z "$ac_lib"; then
     ac_res="none required"
   else
